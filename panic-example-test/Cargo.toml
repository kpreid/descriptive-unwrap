[package]
name = "panic-example-test"
description = "Tests for the text produced by using descriptive-unwrap."
publish = false
edition = "2024"

[[bin]]
name = "panic-example-test"
test = false # no unit tests

[dependencies]
descriptive-unwrap = { path = "../" }

[dev-dependencies]
trycmd = { version = "0.15.11" }