The files in this directory are used in two ways:

* They are embedded in the library documentation as examples.
* They are executed by the tests in the `panic-example-test` module, as a
  `trycmd` test suite, to confirm that the examples are accurate.